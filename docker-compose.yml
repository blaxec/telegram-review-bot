# file: docker-compose.yml

services:
  postgres_db:
    image: postgres:15-alpine
    container_name: my_telegram_bot_postgres
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASS:-password}
      POSTGRES_DB: ${DB_NAME:-telegram_bot_db}
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-telegram_bot_db}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - telegram_bot_net

  redis_db:
    image: redis:7-alpine
    container_name: my_telegram_bot_redis
    ports:
      - "6379:6379"
    restart: unless-stopped
    networks:
      - telegram_bot_net

  bot:
    build: .
    container_name: my_telegram_bot_app
    depends_on:
      postgres_db:
        condition: service_healthy
    env_file:
      - .env
    volumes:
      - .:/app
    # --- ИЗМЕНЕНИЕ ЗДЕСЬ ---
    # Теперь основная команда - это просто запуск бота. Миграции выносятся в отдельную команду.
    command: python main.py
    # -----------------------
    networks:
      - telegram_bot_net

volumes:
  postgres_data:

networks:
  telegram_bot_net:
    driver: bridge